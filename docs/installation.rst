.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pycorm

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pycorm
    $ pip install pycorm
